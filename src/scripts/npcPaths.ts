const npcs = [
  // list of NPCs
  "PoliceDesk",
  "Bartender",
  "Mother",
  "Receptionist",
  "Detective",
  "Landlord",
  "Neighbor1",
];

const paths = npcs.map((x) => `../npcs/${x}`);

export default paths;
